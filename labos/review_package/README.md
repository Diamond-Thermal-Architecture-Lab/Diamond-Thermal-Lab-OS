# Decision Review Package

`labos.review_package` exports the deterministic Decision Board preview into a separate review package for human inspection. It does not write canonical case files, approve architectures, or modify pattern or memory files.

The package contains only `decision_board_preview.md`, `decision_board_preview.json`, `review_manifest.json`, and `human_review_checklist.md`.
